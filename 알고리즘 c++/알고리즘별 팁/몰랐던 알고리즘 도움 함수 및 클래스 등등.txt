 int isdigit(int c);
char타입을 숫자인지 아닌지 판단하여
맞다면 숫자 출력, 아니라면 0반환

 for(시작;어디까지;배열){내용}
배열에 넣은 결과값을
배열에 넣어 재귀 확인 가능

 stoi
string->int 변환

 memset
메모리내용 특정값으로 세팅
헤더파일은 c++에서 cstring

시퀀스컨테이너

입력 받을때
한개안에서 여러요소 받는다면
무조건 곧이 곧대로 받지말고
요소 순서 바꿔서 받는 경우도 생각해보기

1. lower_bound(배열시작,배열끝,찾는거)-배열시작
찾는값과 같거나 큰 값이 몇번째에 등장하는지
2. upper_bound(배열시작,배열끝,찾는거)-배열시작
찾는값을 초과하는 값이 몇번째에 등장하는지
2.-1.하면
찾는값과 동일한 값 갯수 알 수 있음

